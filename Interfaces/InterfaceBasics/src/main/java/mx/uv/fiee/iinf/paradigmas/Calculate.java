package mx.uv.fiee.iinf.paradigmas;

/**
 * Define el comportamiento que todas clases que implementen la interfaz
 * deben cumplir, saber calcualar su área.
 *
 * No se define en la interfaz mx.uv.fiee.iinf.paradigmas.Draw, ya que este comportamientos (métodos) corresponden
 * a distintas operaciones.
 */
public interface Calculate {
    void calculateArea ();
}
